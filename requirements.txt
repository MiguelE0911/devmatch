'''Infraestructura base (Etapa 1). Antes de agregar algo aquí, avisa en el
chat del equipo (ver DEVMATCH-ESTRUCTURA.md, "Propiedad de archivos compartidos").'''
Django>=5.2,<5.3
psycopg2-binary>=2.9,<3.0
python-dotenv>=1.0,<2.0
dj-database-url>=3.1.2

# Se agregan más adelante, cuando la etapa correspondiente los necesite:
# Pillow          -> procesamiento de logos/prototipos (proyecto_media)
# requests        -> integración con la API de GitHub (Etapa 4)